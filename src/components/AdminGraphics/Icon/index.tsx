import React from 'react'

import { Logo } from '@/components/Brand/Logo'

const AdminIcon: React.FC = () => {
  return <Logo style={{ height: '100%', width: '100%', color: 'var(--theme-text)' }} />
}

export default AdminIcon
