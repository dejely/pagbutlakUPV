import React from 'react'

import { Wordmark } from '@/components/Brand/Wordmark'

const AdminLogo: React.FC = () => {
  return <Wordmark style={{ height: '40px', width: 'auto', color: 'var(--theme-text)' }} />
}

export default AdminLogo
